import { getHiscores } from 'osrs-wrapper';
import mainStore from "../../mainStore";


export async function updateRunescapeStats(name) {
	// const stats = await getHiscores(name);
	// console.log(stats);
}
